import 'package:flutter/material.dart';

/// Register page for auth flow.
/// Página de registro del flujo de auth.
class RegisterPageAuth extends StatelessWidget {
  /// Creates a RegisterPageAuth widget.
  /// Crea un widget RegisterPageAuth.
  const RegisterPageAuth({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Register Page Auth'));
  }
}
