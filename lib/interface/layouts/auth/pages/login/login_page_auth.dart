import 'package:flutter/material.dart';

/// Login page for auth flow.
/// Página de inicio de sesión del flujo de auth.
class LoginPageAuth extends StatelessWidget {
  /// Creates a LoginPageAuth widget.
  /// Crea un widget LoginPageAuth.
  const LoginPageAuth({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Login Page Auth'));
  }
}
