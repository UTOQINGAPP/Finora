import 'package:flutter/material.dart';

/// Recover password page for auth flow.
/// Página de recuperación de contraseña del flujo de auth.
class RecoverPasswordPageAuth extends StatelessWidget {
  /// Creates a RecoverPasswordPageAuth widget.
  /// Crea un widget RecoverPasswordPageAuth.
  const RecoverPasswordPageAuth({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Recover Password Page Auth'));
  }
}
