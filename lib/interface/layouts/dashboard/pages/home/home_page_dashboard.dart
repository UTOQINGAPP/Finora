import 'package:flutter/material.dart';

/// Home page for dashboard.
/// Página de inicio del dashboard.
class HomePageDashboard extends StatelessWidget {
  /// Creates a HomePageDashboard widget.
  /// Crea un widget HomePageDashboard.
  const HomePageDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Home Page Dashboard'));
  }
}
