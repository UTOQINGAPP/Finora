import 'package:flutter/material.dart';

/// Transactions page for dashboard.
/// Página de transacciones del dashboard.
class TransactionsPageDashboard extends StatelessWidget {
  /// Creates a TransactionsPageDashboard widget.
  /// Crea un widget TransactionsPageDashboard.
  const TransactionsPageDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Transactions Page Dashboard'));
  }
}
