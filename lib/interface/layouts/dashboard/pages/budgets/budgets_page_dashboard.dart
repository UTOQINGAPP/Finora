import 'package:flutter/material.dart';

/// Budgets page for dashboard.
/// Página de presupuestos del dashboard.
class BudgetsPageDashboard extends StatelessWidget {
  /// Creates a BudgetsPageDashboard widget.
  /// Crea un widget BudgetsPageDashboard.
  const BudgetsPageDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Budgets Page Dashboard'));
  }
}
