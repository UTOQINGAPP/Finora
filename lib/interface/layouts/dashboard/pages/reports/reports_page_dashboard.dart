import 'package:flutter/material.dart';

/// Reports page for dashboard.
/// Página de reportes del dashboard.
class ReportsPageDashboard extends StatelessWidget {
  /// Creates a ReportsPageDashboard widget.
  /// Crea un widget ReportsPageDashboard.
  const ReportsPageDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Reports Page Dashboard'));
  }
}
